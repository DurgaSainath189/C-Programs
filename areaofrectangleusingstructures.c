#include<stdio.h>
struct rectangle{
	int lenght;
	int breadh;
}l,b;
int main()
{
	int x,area1,area2;
	printf("Enter the value of l:");
	scanf("%d",&l.lenght);
	printf("Enter the value of b:");
	scanf("%d",&b.breadh);
	area1=l.lenght*b.breadh;
	printf("The area of rectangle is:%d",area1);
	printf("Enter the value to be incremented:");
	scanf("%d",&x);
	area2=l+x.lenght*b+x.breadh;
	printf("The area of new rectangle is:%d",area2);
	if(area==areax)
	printf("Both the rectangles have same area");
	else
	printf("Both the rrecatngles areas are different");
	return 0;
}
